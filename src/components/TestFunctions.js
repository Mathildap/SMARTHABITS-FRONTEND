export const handleHabitNumber = (value) => {
    let parsedNumber = parseInt(value);
    return parsedNumber;
};

export const emailHandler = (e) => {
    let setEmail = e.toLowerCase();
    return setEmail;
};
