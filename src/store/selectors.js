export const selectHabits = (state) => state.habitReducer;

export const selectUser = (state) => state.userReducer;

export const selectTodos = (state) => state.todosReducer;

export const selectNotes = (state) => state.notesReducer;
